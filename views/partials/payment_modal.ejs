<!-- Payment Modal -->
<div id="paymentModal" class="modal-overlay">
    <div class="modal-glass-card payment-card">
        <div class="modal-content-wrapper">
            <h3>Scan & Pay</h3>
            <p class="text-muted">Scan the QR code to pay via any UPI App</p>

            <div class="qr-wrapper">
                <img src="/images/upi_qr.jpg" alt="UPI QR Code" class="qr-img">
            </div>

            <div class="payment-details">
                <div class="amount-row">
                    <span>Total Amount</span>
                    <span class="pay-amount">Loading...</span>
                </div>
                <div class="upi-id-row">
                    <span>UPI ID: </span>
                    <span class="upi-id">9822699485@kotak811</span>
                </div>
            </div>

            <div class="modal-actions">
                <button class="btn-secondary" onclick="closePaymentModal()">Cancel</button>
                <button class="btn-success" id="confirmPaymentBtn" onclick="confirmPayment()">
                    <i class="fas fa-check-circle"></i> I made the payment
                </button>
            </div>
        </div>
    </div>
</div>

<style>
    .payment-card {
        max-width: 380px;
        padding: 30px;
    }

    .qr-wrapper {
        background: white;
        padding: 15px;
        border-radius: 15px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        margin: 20px auto;
        width: 200px;
        height: 200px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .qr-img {
        width: 100%;
        height: 100%;
        object-fit: contain;
    }

    .payment-details {
        background: rgba(255, 255, 255, 0.5);
        border-radius: 10px;
        padding: 15px;
        margin-bottom: 25px;
        text-align: left;
    }

    .amount-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 1.1rem;
        font-weight: 600;
        margin-bottom: 5px;
        color: #333;
    }

    .pay-amount {
        color: var(--primary-dark);
        font-size: 1.25rem;
    }

    .upi-id-row {
        font-size: 0.85rem;
        color: #666;
        text-align: center;
        background: #f1f2f6;
        padding: 5px;
        border-radius: 5px;
        margin-top: 10px;
    }

    .upi-id {
        font-family: monospace;
        font-weight: 600;
        color: #333;
    }

    .btn-success {
        width: 100%;
        padding: 14px;
        background: #00b894;
        color: white;
        border: none;
        border-radius: 50px;
        font-weight: 600;
        font-size: 1rem;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
        transition: 0.3s;
        box-shadow: 0 5px 15px rgba(0, 184, 148, 0.3);
    }

    .btn-success:hover {
        background: #00a383;
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(0, 184, 148, 0.4);
    }
</style>